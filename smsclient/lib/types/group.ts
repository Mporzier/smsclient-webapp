/** Ligne affichée dans la vue Groupes. */
export type GroupRowData = {
  id: string;
  name: string;
  description: string;
  contactCount: number;
  lastCampaignLabel: string;
  createdLabel: string;
};
