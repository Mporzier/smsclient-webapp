export type LinkRowData = {
  id: string;
  label: string;
  originalUrl: string;
  shortCode: string;
  shortUrl: string;
  clickCount: number;
  createdLabel: string;
  campaignId: string | null;
};
