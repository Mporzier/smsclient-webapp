/** Au-delà : sélection Gmail → filtre serveur, pas N ids en state React. */
export const CAMPAIGN_AUDIENCE_FILTER_THRESHOLD = 100;

export type CampaignEligibleAudienceFilter = {
  search: string;
};
